AWSTemplateFormatVersion: '2010-09-09'
Description: Security Groups - ALB + ECS

Parameters:
  Env:
    Type: String
    AllowedValues: [dev, qa, uat, prod]
  Workstream:
    Type: String
    Description: Workstream identifier
  ProjectName:
    Type: String
    Description: Application name
  VpcId:
    Type: AWS::EC2::VPC::Id

Resources:
  AlbSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupName: !Sub 'hq-${Workstream}-${Env}-${ProjectName}-SG-ALB'
      GroupDescription: Allow inbound HTTP to ALB
      VpcId: !Ref VpcId
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
      Tags:
        - Key: Name
          Value: !Sub 'hq-${Workstream}-${Env}-${ProjectName}-SG-ALB'
        - Key: Workstream
          Value: !Ref Workstream
        - Key: Project
          Value: !Ref ProjectName
        - Key: Env
          Value: !Ref Env

  EcsSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupName: !Sub 'hq-${Workstream}-${Env}-${ProjectName}-SG-ECS'
      GroupDescription: Allow inbound from ALB to ECS tasks
      VpcId: !Ref VpcId
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 8080
          ToPort: 8080
          SourceSecurityGroupId: !Ref AlbSecurityGroup
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
      Tags:
        - Key: Name
          Value: !Sub 'hq-${Workstream}-${Env}-${ProjectName}-SG-ECS'
        - Key: Workstream
          Value: !Ref Workstream
        - Key: Project
          Value: !Ref ProjectName
        - Key: Env
          Value: !Ref Env

Outputs:
  AlbSecurityGroupId:
    Value: !Ref AlbSecurityGroup
  EcsSecurityGroupId:
    Value: !Ref EcsSecurityGroup
